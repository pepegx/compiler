##  Структура проекта

```
compiler/
├── lexer/LexerApp/          # Лексический анализатор
├── syntaxer/SyntaxerApp/    # Парсер + семантический анализ
│   ├── Parser/              # Синтаксический анализатор
│   ├── AST/                 # Абстрактное синтаксическое дерево
│   ├── Analyzer/            # Семантический анализатор + оптимизатор
│   └── Utilities/           # Утилиты (вывод AST)
└── testsO/                  # Тестовые программы
    ├── positive/            # Корректные программы
    ├── negative/            # Программы с ошибками
    ├── checks/              # Тесты семантических проверок
    └── optimizations/       # Тесты оптимизаций
```

---

## ✨ Возможности

### 🔍 Семантический анализатор

- Проверка дубликатов (классы, методы, переменные)
- Проверка использования переменных до объявления
- Валидация контекста (`return`, `this`)
- Проверка существования базовых классов
- Проверка типов

### ⚡ Оптимизатор AST

- Удаление неиспользуемых переменных-членов класса
- Удаление неиспользуемых локальных переменных
- Удаление недостижимого кода после `return`
- Удаление циклов `while(false)`
- Упрощение `if(true)` и `if(false)`

---

 

## 🚀 Запуск

### Из корня проекта

```bash
dotnet run --project syntaxer/SyntaxerApp/SyntaxerApp.csproj -- testsO/positive/01_empty.o
```

### Из папки SyntaxerApp

```bash
cd syntaxer/SyntaxerApp
dotnet run -- ../../testsO/positive/01_empty.o
```

### Без оптимизаций

```bash
dotnet run --project syntaxer/SyntaxerApp/SyntaxerApp.csproj -- testsO/positive/01_empty.o --no-optimize
```

---

## 🧪 Тесты

### Запуск тестов вручную

```bash
# Корректная программа
dotnet run --project syntaxer/SyntaxerApp/SyntaxerApp.csproj -- testsO/positive/03_constructor.o

# Программа с синтаксической ошибкой
dotnet run --project syntaxer/SyntaxerApp/SyntaxerApp.csproj -- testsO/negative/17_no_end.o

# Тест семантической проверки (должна быть ошибка)
dotnet run --project syntaxer/SyntaxerApp/SyntaxerApp.csproj -- testsO/checks/01_duplicate_class.o

# Тест оптимизации
dotnet run --project syntaxer/SyntaxerApp/SyntaxerApp.csproj -- testsO/optimizations/01_unused_member_vars.o
```

### Категории тестов

**`testsO/positive/`** - корректные программы (~25 файлов)  
**`testsO/negative/`** - программы с синтаксическими ошибками  
**`testsO/checks/`** - программы с семантическими ошибками  
**`testsO/optimizations/`** - программы для проверки оптимизаций  

---

 

## 🛠️ Технологии

- **C# .NET 9.0**
- **Рекурсивный спуск** (парсинг)
- **AST** (представление программы)
- **Двухфазный анализ** (проверки → оптимизации)

---