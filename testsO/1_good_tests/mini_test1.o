class MainClass is
    method main : Integer is
        var n   : Integer(5)
        var acc : Integer(1)
        while n.Greater(Integer(1)) loop
            acc := acc.Mult(n)
            n := n.Minus(1)
        end
        return acc.Plus(Integer(3)) 
    end
end
