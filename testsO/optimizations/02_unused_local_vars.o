class TestOpt2 is
    method calculate() : Integer is
        var result : Integer(0)
        var unused1 : Integer(1)
        var unused2 : Integer(2)
        var unused3 : Integer(3)
        
        result := Integer(42)
        return result
    end
end
