class TestOpt4 is
    var counter : Integer(0)
    
    method test() is
        while false loop
            counter := Integer(100)
            counter := counter.Plus(Integer(1))
        end
        
        counter := Integer(5)
    end
end
