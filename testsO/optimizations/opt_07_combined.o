class ComplexOptimizations is
    var usedField : Integer(0)
    var unusedField1 : Integer(1)
    var unusedField2 : Integer(2)
    
    method combo1() : Integer is
        var used : Integer(5)
        var unused1 : Integer(10)
        var unused2 : Integer(20)
        
        if true then
            return used
        else
            while false loop
                unused1 := Integer(100)
            end
            return Integer(999)
        end
        
        used := Integer(888)
    end
    
    method combo2() is
        var temp : Integer(0)
        
        if false then
            temp := Integer(50)
        else
            usedField := Integer(10)
        end
        
        while false loop
            usedField := Integer(999)
        end
    end
    
    method combo3() : Boolean is
        var result : Boolean(true)
        var notUsed : Integer(0)
        
        if true then
            if false then
                return Boolean(false)
            else
                return result
            end
        else
            return Boolean(false)
        end
        
        result := Boolean(false)
    end
end
