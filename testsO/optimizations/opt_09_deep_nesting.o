class DeepNesting is
    var field : Integer(0)
    
    method deeplyNested() : Integer is
        var a : Integer(1)
        var unused1 : Integer(10)
        
        if true then
            var b : Integer(2)
            var unused2 : Integer(20)
            
            while false loop
                var c : Integer(3)
                if false then
                    field := Integer(999)
                else
                    field := Integer(888)
                end
            end
            
            if true then
                return a.Plus(b)
            else
                return Integer(0)
            end
        else
            return Integer(777)
        end
        
        field := Integer(666)
    end
end
